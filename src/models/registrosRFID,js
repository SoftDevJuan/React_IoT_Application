//const mongoose = require('mongoose');
import mongoose from "mongoose";

const registroRFIDSchema = new mongoose.Schema({
    rfidnumber : {
        type: String,
        required: true
    }
   
   
}, {timestamps : true})



export default mongoose.model('registroRFID', registroRFIDSchema);
